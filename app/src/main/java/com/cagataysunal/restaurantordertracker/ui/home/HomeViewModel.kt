package com.cagataysunal.restaurantordertracker.ui.home

import androidx.lifecycle.ViewModel

class HomeViewModel : ViewModel() {
    // This ViewModel is currently not used for any specific logic on the home screen itself,
    // but can be used in the future if needed.
}